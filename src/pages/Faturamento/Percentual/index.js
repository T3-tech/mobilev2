import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default (pros) => {
    return (
        <SafeAreaView>
            <Text>Oi Percentual</Text>
        </SafeAreaView>
    );
};
